
Pre-requisites :Install node.js on the machine -> go to node.js command prompt, 
          navigate to source code folder and type -->  node server.js to run the program

1).node.js command prompt will log the request Method, url and time.
2).Go to browser and type localhost:3000 ---> 3000 is the port number
3). type localhost:3000/about
4). To stop the server from running type ^C (ctrl+shift+ ^ +C).

